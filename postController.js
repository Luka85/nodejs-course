const posts = [
  { id: 1, title: "PostOne" },
  { id: 2, title: "PostTwo" },
];

const getPosts = () => posts;

// export { getPosts };

export const GetPostsLength = () => posts.length;

export default getPosts;
